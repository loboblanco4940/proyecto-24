/*class Rubber {
	constructor(x, y, radio) {
	  var options = {
		  restitution:0.3,
		  friction:5,
		  density:1
	  }
	  this.body = Bodies.circle(200, 100, 10);
	  this.width = width;
	  this.height = height;
	  
	  World.add(world.rubber);
	}
	display(){
	
	  var pos =this.body.position;
	  var angle = this.body.angle;
	  push();
	  translate(pos.x, pos.y);
	  rotate(angle);
	  ellipseMode(RADIUS);
	  
	  fill('blue');
	  ellipse(0, 0, this.r, this.r);
	  pop();
	}
  }
*/
